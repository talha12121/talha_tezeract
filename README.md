# First you need to run the command:
```npm i ```

# Technologies used:
* React 
* Tailwind CSS
* Firebase For Image (but it was creating problem during installation)
* zod and react-hook-form for form validation

# Application Flow:
* You can create a new user 
* Users data is stored in local storage.
* You can see all users in user table page.
